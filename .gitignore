# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# secretos locales que nunca deben salir del equipo
# (.env.local.bak no lo cubría el patrón de arriba, y el xlsx trae accesos)
*.bak
Plataformas access and data.xlsx

# vercel (project.json se commitea para fijar el proyecto correcto)
.vercel/*
!.vercel/project.json

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/dev.db
prisma/dev.db-journal

# npm cache
.npm-cache/

# firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Binario compilado de tools/pime-audio (se regenera con npm run audio)
tools/pime-audio/pime-audio

# impeccable: la config de excepciones se comparte; la caché del hook no.
.impeccable/hook.cache.json

# Restos de trabajo local que no van al repo (es público): pruebas de PDF con
# datos de clientes, logs de recuperación de disco, capturas de las pruebas del
# navegador y el ledger de verificación.
scratch-*
photorec.*
.playwright-mcp/
GATES.md
*.code-workspace
login-light.png
